{{!-- main.handlebars file --}}
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Present Tracker</title>
  <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>

<body>
  <header>
    <h1>Birthday Present Tracker</h1>
    <h2>Remembers all the gifts you think of so you don't have to</h2>
  </header>
  <nav>
    <a href="/">Home</a>
    {{#if loggedIn}}
    <button id="logout" class="btn-no-style">Logout</button>
    {{else}}
    <a href="/login">Login</a>
    {{/if}}

    <a href="/new-gift">New Gift</a>
    <a href="/new-person">New Person</a>
  </nav>
  <main>
    {{!-- <aside>
      <h2>Enjoy the galleries!</h2>
    </aside> --}}
    <div class="gallery">
      {{{body}}}
    </div>
  </main>
  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>